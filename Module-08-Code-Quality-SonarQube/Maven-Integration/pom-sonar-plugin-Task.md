# Task Description

Configure Maven to run SonarQube analysis. Add sonar-maven-plugin to pom.xml build/plugins section. Configure JaCoCo plugin for code coverage reporting (prepare-agent, report goals). Show the command to run analysis: mvn clean verify sonar:sonar -Dsonar.token=YOUR_TOKEN. Include properties for JaCoCo output directory and report path. Explain how the Maven lifecycle integrates compilation, testing, coverage, and analysis in one command.
