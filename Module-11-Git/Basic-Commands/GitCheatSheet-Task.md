# Task Description

Create a comprehensive Git commands reference document. Cover: setup (config user.name, user.email), repository creation (init, clone), daily workflow (status, add, commit, push, pull), branching (branch, checkout, switch, merge, rebase), inspection (log, diff, show, blame), undoing changes (reset, revert, checkout, clean), stashing (stash push, pop, list, drop), remotes (remote add, remove, set-url). For each command, include common flags and a real-world example scenario.
