# Task Description

Configure VS Code debugger for a React application. Create .vscode/launch.json with Chrome debug configuration. Set breakpoints in JSX and JavaScript files. Use Step Over (F10), Step Into (F11), Step Out (Shift+F11). Inspect variables in Watch panel and Locals panel. Evaluate expressions in Debug Console. Show conditional breakpoints (break only when variable meets condition). Demonstrate debugging a React component'\''s state changes by setting breakpoints in useEffect and event handlers.
