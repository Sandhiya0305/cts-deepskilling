# Task Description

Configure Spring Boot to support multiple content types. Enable JSON (default) and XML responses. Add Jackson XML dependency (jackson-dataformat-xml). Use @RestController with produces = {MediaType.APPLICATION_JSON_VALUE, MediaType.APPLICATION_XML_VALUE}. Demonstrate client requesting JSON (Accept: application/json) vs XML (Accept: application/xml). Configure custom media type if needed. Show how content negotiation works with HttpMessageConverters.
