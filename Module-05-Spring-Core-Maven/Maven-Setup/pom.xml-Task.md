# Task Description

Create a Maven pom.xml for a Spring Core project from scratch. Include: project coordinates (groupId, artifactId, version, packaging as jar), properties section with Java version 17 and Spring version 6.0.x, dependencies for spring-context, spring-core, junit-jupiter (for testing), mockito-core (for mocking), and logback-classic (for logging). Include build plugins: maven-compiler-plugin (source and target 17), maven-surefire-plugin (for running tests). Show dependency management section if using a BOM. Explain each section'\''s purpose in comments.
