# Task Description

Document essential Docker commands. Images: docker build, docker images, docker rmi, docker pull, docker push. Containers: docker run (-d, -p, -v, -e, --name), docker ps, docker stop, docker start, docker rm, docker exec, docker logs. Networks: docker network ls, docker network create. Volumes: docker volume ls, docker volume create, docker volume rm. Compose: docker-compose up, down, build, logs, exec. Include cleanup commands for removing unused resources. Add a troubleshooting section for common issues.
