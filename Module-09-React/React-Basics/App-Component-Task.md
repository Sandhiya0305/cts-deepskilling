# Task Description

Create a basic React application structure. Use create-react-app or Vite to scaffold the project. Create App.js as the root component. Include: a Header component with navigation links, a Main content area using React Router, and a Footer component. Demonstrate JSX syntax, component composition, and props passing. Use functional components with arrow functions. Include inline styles and CSS module imports. Show how React renders the virtual DOM to the real DOM.
