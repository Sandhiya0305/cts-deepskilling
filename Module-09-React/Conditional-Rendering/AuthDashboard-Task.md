# Task Description

Create a component demonstrating conditional rendering. Show: element variables (assign JSX to variable based on condition), inline if with logical &amp;&amp; operator (show notification only if hasNotification), inline if-else with ternary operator (show Login or Logout button), preventing component rendering by returning null. Create an AuthDashboard that shows AdminPanel if user.role === '\''ADMIN'\'', UserProfile if '\''USER'\'', and AccessDenied otherwise. Demonstrate early return pattern for guard clauses.
