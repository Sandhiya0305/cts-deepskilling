# Task Description

Create a component that renders a list of employees in a table. Use array.map() to transform data into JSX. Include a unique key prop for each row (use employee.id, never index). Show extracting a TableRow component. Demonstrate sorting the list by clicking column headers. Include filtering with a search input. Handle empty list state with a friendly message. Show how keys help React identify which items changed for efficient re-renders.
