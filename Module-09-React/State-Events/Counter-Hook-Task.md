# Task Description

Create a Counter component using useState hook. Features: increment button, decrement button, reset button, step size input (controlled component). Show functional update form of setState: setCount(prev =&gt; prev + 1) for reliable updates based on previous state. Add event handlers with preventDefault where needed. Demonstrate how React batches state updates. Include a useEffect that logs count changes to console. Show cleanup function in useEffect if needed.
